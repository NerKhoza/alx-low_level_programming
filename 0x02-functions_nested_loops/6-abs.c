#include "main.h"
/**
 * _abs - function that takes the absolute value of a number
 * @x: function parameter
 * Return: -a or a
 */

int _abs(int x)
{
	if (x < 0)
		return (-x);
	else
		return (x);
}
